Tests your package lock library for known Shai-Hulud 2.0 infected packages.
Infected packages are retreived dynamically but can run with repo's saved infections as a backup.

Run it with:
```python
python check-shai-hulud.py <path/to/package-lock.json>
```